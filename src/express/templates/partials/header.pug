header(class={header: true, "header--logged": user})
  a.header__logo.logo(href='/')
    img.logo__image(src='/img/icons/logo-image-only.svg' alt='логотип Тайпотеки')
    span.logo__name Типотека
  if !user
    nav.header__nav
      ul.header__nav-list
        li.header__list-item
          a.header__registration(href='/register') Регистрация
        li.header__list-item
          a.header__enter(href='/login') Вход
  a.header__search.button.button--search(href='/search' aria-label='поиск по сайту')
  if user
    img.header__avatar(src=`/img/${user.avatar || `avatar-sample.png`}` alt="Аватар пользователя")
    p #{user.name}
    a.header__enter(href="/logout") Выход
if user && user.isOwner
  nav.header.owner-menu
    ul.header__nav-list
      li.header__list-item
        a.header__registration(href='/my') Мои записи
      li.header__list-item
        a.header__registration(href='/categories') Категории
      li.header__list-item
        a.header__registration(href='/my/comments') Все комментарии
      li.header__list-item
        a.header__enter(href='/articles/add') Новая запись
